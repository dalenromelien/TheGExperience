import React from "react";

const Event = (backgroundColor, events) => {
  return (
    <>
      <div className="event-conatiner">{events.map((event) => {})}</div>
    </>
  );
};

export default Event;
